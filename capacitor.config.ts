import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'google-maps-capacitor-community',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
